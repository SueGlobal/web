<h1>Listing employer_studies</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employer_studies.each do |employer_study| %>
  <tr>
    <td><%= link_to 'Show', employer_study %></td>
    <td><%= link_to 'Edit', edit_employer_study_path(employer_study) %></td>
    <td><%= link_to 'Destroy', employer_study, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employer study', new_university_employer_study_path(@university) %>
