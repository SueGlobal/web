<fieldset>
  <legend><%= t 'legend', scope: 'study.basic_question_form' %></legend>

  <%= f.input :title, as: :string %>
  <%= f.input :author, as: :string %>
  <%= f.input :year, as: :course %>

</fieldset>
