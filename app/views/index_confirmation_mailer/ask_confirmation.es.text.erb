Se ha propuesto la creación del siguiente indicador:

===========================
<%= @index.name %>
<%= @index.description %>
===========================

Para aceptar siga el siguiente enlace:

<%= @confirm_url %>

Si no está de acuerdo y quiere evitar que se cree,
siga este enlace:

<%= @reject_url %>
