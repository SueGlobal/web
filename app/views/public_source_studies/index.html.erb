<h1><%= t '.header', university: @university.abbreviation %></h1>

<%= render partial: 'index_table',
            locals: { studies: @public_source_studies,
                      klass: PublicSourceStudy } %>
<br />

<%= link_to_if can?(:edit, @university), t('.new_public_source_study'), new_university_public_source_study_path(@university), :class => 'btn' do "" end %>
