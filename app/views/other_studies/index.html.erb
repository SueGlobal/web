<h1><%= t '.header', university: @university.abbreviation %></h1>

<%= render partial: 'index_table',
            locals: { studies: @other_studies,
                      klass: OtherStudy } %>

<br />

<%= link_to_if can?(:new, OtherStudy), t('.new_other_study'), new_university_other_study_path(@university), :class => 'btn' do "" end %>
